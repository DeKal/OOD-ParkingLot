package size;
import consts.Const.Size;

public class SizeCompare {
    public static boolean compareVehicleToSpotSize(Size vehicleSize, Size spotSize) {
        return vehicleSize.equals(spotSize);
    }
}
